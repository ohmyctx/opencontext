# Browser Collectors

Browser activity collectors for Chrome, Firefox, and Edge (all Chromium-based; Firefox uses its own MV3-compatible implementation).

For installation instructions, see [INSTALL.md](INSTALL.md).